lista_numeros = []
quadrado_numeros = []

for i in range(1,16):
    numero = int(input(f"digite o {i} número: "))
    lista_numeros.append(numero)  
    
quadrado_numeros = [n ** 2 for n in lista_numeros   ]

print(f"A lista dos quadrados dos números é {quadrado_numeros}:")
