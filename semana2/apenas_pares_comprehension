lista_numeros = []
lista_pares = []

for i in range(1,16):
    numero = int(input(f"digite o {i} número: "))
    lista_numeros.append(numero)  

lista_pares = [n for n in lista_numeros if n % 2 == 0]

print(f"A lista dos quadrados dos números é {lista_pares}:")